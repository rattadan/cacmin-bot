version: '3.8'

services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ./data:/usr/src/app/data
      - ./logs:/usr/src/app/logs
    restart: unless-stopped
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge
