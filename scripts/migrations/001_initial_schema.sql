-- Initial database schema migration
-- This migration ensures all tables have the latest structure

-- No changes needed as initDb() already creates the correct schema
-- This file serves as an example for future migrations

-- Example future migration would look like:
-- ALTER TABLE users ADD COLUMN new_field TEXT;
-- CREATE INDEX idx_new_field ON users(new_field);
